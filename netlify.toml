[build]
  publish = "."
  functions = "netlify/functions"

# Makes /api/webhook map to netlify/functions/webhook.js, etc.
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
